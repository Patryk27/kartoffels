use crate::views::game::Event;
use kartoffels_ui::Ui;

#[derive(Default)]
pub struct SpawnPrefabDialog {
    //
}

impl SpawnPrefabDialog {
    pub fn render(&mut self, _ui: &mut Ui<Event>) {
        // ui.info_window(width, height, Some(" spawn prefab "), |ui| {

        // });
    }
}
